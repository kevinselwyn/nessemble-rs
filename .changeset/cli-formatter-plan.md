---
nessemble: none
---
Add `plans/005-formatter.md`, a planning document for a prettier-style built-in `nessemble format` subcommand and an optional `.nessemblerc` JSON config, building on the existing lossless-lexer `tooling::format` engine. Planning only — no shipped behavior changes.
