---
nessemble: none
---
Document the `nessemble format` command and the `.nessemblerc` configuration file in the manual (`docs/src/usage.md`), and note in the editor guide that the language server's "format document" runs the same engine as the CLI. Phase 5 of `plans/005-formatter.md`.
