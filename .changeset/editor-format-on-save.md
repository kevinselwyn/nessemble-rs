---
nessemble: none
---
Document how to enable format-on-save for the `nessemble` language server in VS Code / Cursor (`docs/src/editor.md`), and give the example extension a stable `publisher` so `editor.defaultFormatter` has an identifier to reference.
