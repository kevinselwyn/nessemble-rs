---
nessemble: none
---
Require a changeset on every PR: add a CI presence gate (with a `no-changeset`
label / `nessemble: none` opt-out) plus a step that validates pending changesets.
CI-only (plan 004, Phase 2); no shipped-behavior change.
