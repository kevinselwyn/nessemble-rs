---
nessemble: none
---
Add plan 010 designing routine signature annotations: JSDoc-shaped comment directives (`@nessemble-param`, `@nessemble-returns`, `@nessemble-clobbers`) that document a subroutine's register calling convention, plus the call-site hover, lint validation, and static clobber verification built on them (planning only; no shipped behavior changes).
